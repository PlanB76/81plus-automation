name: EMAILFLUX81 scheduler
on: {schedule: [{cron: "*/20 * * * *"}], workflow_dispatch: {}}
jobs: {tick: {runs-on: ubuntu-latest, steps: [{name: cron, run: "curl -fsS --max-time 120 \"${{ secrets.EMAILFLUX_CRON_URL }}\" || echo no-url"}]}}
